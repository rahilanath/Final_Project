name: Final_Project
# Install: conda env create --file final_project.yml
# Update:  conda env update --file final_project.yml --prune
dependencies:
  - python=3.6
  - numpy
  - pandas
  - requests
  - scipy
  - scikit-learn
  - tensorflow
  - py4j
  - pyspark
  - jupyter_client
  - nb_conda_kernels
  - matplotlib
  - keras
  - ipykernel
  
# In addition to Python_Adv
  - nltk
  - seaborn
  - gensim

# install lime:
# pip install lime
# Also, you need to download 'stopwords', 'wordnet', 'punkt', 'averaged_perceptron_tagger' for nltk--the way I did it was
# after activating this environment (conda activate Final_Project), 
# python
# >>>> import nltk
# >>>> nltk.download('stopwords')
# >>>> nltk.download('wordnet')
# >>>> nltk.download('punkt')
# >>>> nltk.download('averaged_perceptron_tagger')
